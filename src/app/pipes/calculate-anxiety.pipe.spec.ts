import { CalculateAnxietyPipe } from './calculate-anxiety.pipe';

describe('CalculateAnxietyPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateAnxietyPipe();
    expect(pipe).toBeTruthy();
  });
});
